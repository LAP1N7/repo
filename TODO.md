# 남은 일

우선순위는 **제출 요건 > 완성도 > 확장**이다. 공모전은 하나라도 빠지면 심사 제외라
아래 A 그룹이 무조건 먼저다.

---

## A. 제출 블로커 (이거 없으면 심사 자체가 안 됨)

| 일 | 상태 | 비고 |
|---|---|---|
| git 저장소 생성 + 커밋 기록 | **완료** | 초기 커밋 164파일 |
| `web/` → `docs/` 이름 변경 | **완료** | 익스포트 프리셋도 `docs/index.html` 로 |
| 웹 셸 로딩 화면 (PROJECT RECLAIM + TIP) | **완료** | `build_web.ps1` 로 빌드 |
| GitHub 원격 저장소 + push | **완료** | `github.com/LAP1N7/repo` (master) |
| GitHub Pages 켜기 | **완료** | https://lap1n7.github.io/repo/ · master / docs |
| 플레이 영상 30~60초 | 미착수 | 구성안은 `SUBMISSION.md` §2 |
| 게임 소개 PDF | 초안 완료 | `SUBMISSION.md` §3 → 링크 채우고 변환 |
| AI 활용 기술 PDF | 초안 완료 | `SUBMISSION.md` §4 → **프롬프트 내역 보강 필요** |

`.gitignore` 에서 `*.import` 를 빼야 한다. Godot 4 의 `.import` 는 리소스 UID 를
들고 있어서 빠지면 클론했을 때 씬 참조가 어긋날 수 있다.

### 웹 배포 메모

- **스레드를 끈 채로 내보낸다** (`variant/thread_support=false`). 켜면 브라우저가
  SharedArrayBuffer 를 요구하고, 그러면 COOP/COEP 헤더가 필요한데 GitHub Pages 는
  헤더를 못 준다. 서비스워커 우회는 첫 로드가 한 번 더 돌아서 느리다.
- `docs/.nojekyll` 이 있어야 한다. Jekyll 은 밑줄로 시작하는 파일을 배포에서 뺀다.
- 음악은 반드시 `.wav`. 웹은 `default_playback_type.web = 1`(Sample) 이라 스트리밍
  음원(mp3·ogg)이 **소리 없이 죽는다.** 데스크톱에서는 멀쩡해서 안 잡힌다.
- 배포 확인: `curl -sI https://lap1n7.github.io/repo/index.pck` 가 200 이고
  Content-Length 가 로컬 `docs/index.pck` 와 같아야 한다.

---

## B. 게임 완성도

### B-1. 아트 (없어도 돌아가지만 인상이 크게 갈림)

우선순위 순. 규격은 전부 `ASSETS.md` 에 있다.

1. **유닛 스프라이트 6종** (192×240, 투명) — 지금은 색 원이다. 체감 차이가 제일 큼
2. **튜토리얼 초상 1장** — 궁수 (840×1440). 튜토리얼 전체가 이거 하나로 돈다
3. **궁극기 컷인 6종** (1080×1440) — 전투당 1회뿐이라 매번 특별한 순간
4. 카드 배너 18종 — 없어도 카드는 읽힌다. 마지막에

> 유닛 스프라이트 하나만 들어와도 그 유닛만 그림으로 바뀐다.
> 에셋 슬롯이 `.tscn` → `.png` → 도형 순으로 폴백하므로 하나씩 넣어도 안 깨진다.

### B-2. 효과음

- 7종 제작자 제작 완료 / 6종은 코드 합성 (`hit` `heal` `defend` `buy` `victory` `death`)
- 합성음은 임시 대역이다. 톤을 일부러 건조하게 잡아 뒀다 — 그럴듯하면 교체를 미루게 되므로
- 교체 규격: `assets/sfx/README.md`

### B-3. 스토리 (사용자가 나중에 지시 예정)

**튜토리얼은 고정.** AI 층위를 얹지 않는다.
넣는다면 **스테이지 클리어 후 짧은 텍스트** 형태.

정리해 둔 세계관 메모:

- 지상은 전쟁 병기와 가혹한 환경으로 죽었고 사람들은 지하로 내려갔다
- 지상 탈환에는 전쟁이 필요한데 전략·전술·지식이 전부 소실됐다
- 그래서 만든 것이 이 프로젝트 — 남은 사람들의 지성을 모으는 게임형 데이터 수집
- 주체는 완전한 지능이 아닌 **보조 에이전트**. 시설 유지보수 담당이라 권한이 적고,
  메인 시설은 붕괴했다. 진짜 지시는 인간이 해야 한다
- 잠든 사람이 가상의 지휘관이 되어 전략을 익히고, 우수한 지휘관을 지상 탈환에 투입
- **뇌가 알아서 각자에게 합당한 스토리를 붙인다** — 이세계물로 보든 전장으로 보든

이 설정이 이미 만들어진 메커니즘을 정당화한다:

| 메커니즘 | 설정이 설명하는 것 |
|---|---|
| 난수 0 · 완전 결정론 | 시뮬레이션이니까. 같은 지시 → 같은 결과여야 데이터가 된다 |
| 카드로 규칙을 짠다 | 수집 대상이 조작 실력이 아니라 **판단**이니까 |
| 죽으면 처음부터 | 다회 플레이로 익히는 게 목적. 죽음이 손실이 아님 |
| 도형·격자 추상 렌더 | 화면은 **가공 전 시뮬레이션 레이어**. 해석은 뇌가 붙인다 |

마지막 항목이 중요하다. 추상 비주얼이 "에셋이 없어서"가 아니라 **설정상 맞는 그림**이
된다. 이걸 밀면 궁극기 컷인만 "해석이 붙은 순간"으로 연출할 수 있다 —
평소엔 도형, 결정적 순간에만 사람 얼굴.

**정할 것** — AI 이름/말투, 그리고 실패 데이터를 AI가 (A) 인정하는가 (B) 숨기는가.

---

## C. 밸런스 — 현재 수치와 조정 지점

전부 실측 기반이다. 재려면 `test/runsim.gd`(런 단위) · `test/montecarlo.gd`(단일 전투)
· `test/economy.gd`(구매력) 를 돌린다.

### 현재 상태

```
스테이지 승률   1: 81.9%  2: 61.0%  3: 60.6%  4: 66.1%  5: 56.9%
런 클리어율     최고 12.5% (혼성 · 합성 우선 · 강화 보상)
스테이지 예산   10 → 17 → 26 → 37 → 50
살 수 있는 장수  4.0 → 5.5 → 6.8 → 8.6 → 9.7  (슬롯 9칸)
```

### 조정 노브 (전부 한 줄 상수)

| 상수 | 위치 | 의미 |
|---|---|---|
| `STAGE_BUDGET` | `run_state.gd` | 스테이지별 시작 예산 |
| `REWARD_BUDGET` / `REWARD_TOKENS` | `run_state.gd` | 클리어 보상 |
| `SPEED_BASE_TICKS` / `SPEED_CAP` | `run_state.gd` | 신속 제압 보너스 |
| `SPECIAL_CHANCE_PCT` | `run_state.gd` | 궁극기 상점 등장률 (스테이지별 %) |
| `BUY_SURCHARGE` / `REROLL_COST` | `run_state.gd` | 구매 가산금 · 리롤 누진 |
| `MERGE_COPIES` / `MERGE_COST` | `cards.gd` | 합성 장수 · 비용 |
| `TIER_COPIES` | `cards.gd` | 티어별 상점 등장 장수 |
| `UPGRADE_STEP_PCT` | `run_state.gd` | 강화 1단계 상승률 |
| `growth_hp` / `growth_atk` | `units.gd` | 유닛별 성장 방향 |
| `STALL_LIMIT` | `battle.gd` | 무피해 정체 판정 틱 |

### 아직 안 푼 것

1. **누적 손패 34.8장** (5스테이지) — 예산이 늘면서 카드도 쌓인다. 슬롯은 9칸뿐.
   합성이 런당 3~4회 소모하지만 부족하다.
   후보: 정제권 증량 / 손패 상한 / 합성 비용 인하. **"고르는 재미"로 볼 수도 있어 보류.**
2. **후반형(암살자) 편성 클리어율 1.7%** — 궁극기 없이는 1스테이지가 벽이다.
   유닛이 약해서가 아니라 비영천참이 1스테이지 상점에 5%로만 뜨기 때문.
   *제대로 갖추면 전 구간 100%* 라는 것은 측정으로 확인됐다.
   후보: 1스테이지 궁극기 확률 소폭 상향 / 첫 보상에 궁극기 보장.
3. **희귀 보상 < 강화 보상** — 자동 합성으로 좁혔지만 여전히 강화가 유리하다.
4. **타임아웃 대신 정체 패배** — MAX_TICKS(60) 도달률 1.3%. 사실상 정체 판정이 전담.

---

## D. 전술 — 추가 후보

현재 카드 18종 · 궁극기 6종. 어휘 분포를 보고 빈 축에 넣는 방식으로 늘려 왔다.

### 이미 채운 축

- **조율** (누구를 향할까) — 호위 · 협공 · 광전사 · 암살
- **국면** (언제부터 다르게) — 총력전(12틱 이후) · 복수(아군 위기)

### 아직 빈 축

| 축 | 없는 것 | 예시 카드 |
|---|---|---|
| **적의 상태** | 거리 외에 적을 보는 조건이 없다 | `적 HP < 30% → 추격` |
| **적의 수** | 몇 명 남았는지 못 본다 | `적 1명 남으면 → 전원 집중` |
| **아군 수** | 몇 명 살아 있는지 못 본다 | `아군 1명 남으면 → 결사` |
| **위치** | 격자 좌표를 조건으로 못 쓴다 | `내 진영 안이면 → 방어` |

넣을 때 **반드시 감사(`test/audit.gd`)를 돌릴 것.** 조건이 기존 카드와 겹치거나
지배당하면 잡아낸다. 지금까지 이걸로 잡은 것: 중복 3건 · 지배 1건 · 죽은 규칙 3건.

### 규칙 층위 후보 (카드가 아니라 엔진)

- **압박 규칙** — "30틱 이후 모든 피해 +10%씩 누적". 정체 규칙으로도 안 잡히는
  장기전을 끝낸다. 전체 밸런스에 영향이 커서 승인 대기 중.

---

## E. 검사 체계 (지금 7종)

| 검사 | 무엇을 보는가 | 규모 |
|---|---|---|
| `parse_check` | 모든 `.gd` 가 파싱되는가 | 43개 스크립트 |
| `glyph_check` | 화면 문자가 두 폰트에 다 있는가 | 459종 |
| `text_check` | 문구가 전부 `ui_text.json` 과 연결됐는가 | 75개 |
| `view_test` | 화면이 눌리는가 (그려지는가 아님) | 24개 |
| `headless_test` | 전투 규칙 | 156개 |
| `run_test` | 덱·경제·보상 | 142개 |
| `audit` | 카드 중복·지배·죽은 규칙 | 지적 0건 |

**검사가 이렇게 늘어난 이유**는 반복해서 같은 종류의 버그가 나왔기 때문이다 —
*오류도 안 나고 화면도 멀쩡한데 동작만 안 하는* 것. 그래서 새 검사는 전부
**선언한 값과 실제 동작을 대조**하는 형태로 짰다.

한 줄로 다 돌리려면:

```powershell
foreach ($t in @("parse_check","glyph_check","text_check","view_test","headless_test","run_test","audit")) {
  godot --headless --path . --script "res://test/$t.gd"
}
```
