class_name Threat

## 위협도 — 적이 누구를 칠지 정하는 값.
##
## ── 왜 필요했나 ──────────────────────────────────────────────────────────
## 예전에는 각 대원이 자기 규칙으로 독립적으로 표적을 골랐다. 그래서 "누가
## 맞고 있는가" 라는 개념이 게임 안에 아예 없었다.
##
## 그 결과 두 가지가 동시에 막혔다.
##   표현 - "방패병이 붙잡는 동안 원거리가 갉는다" 를 짤 방법이 없다
##   진단 - 궁수가 왜 죽었는지 알 수 없으니 다음에 뭘 고칠지도 모른다
##
## 포킹·희생·정석 같은 전술은 전부 "어그로를 누가 끄느냐" 에 달려 있는데,
## 그 축이 없으니 결국 모든 판이 닥돌 하나로 수렴했다.
##
## ── 규칙 ─────────────────────────────────────────────────────────────────
## 전부 정수 합산이고 난수는 없다. 같은 배치 + 같은 알고리즘 = 같은 결과가
## 이 프로젝트의 코어 계약이라, 위협도도 예외일 수 없다.
##
## 값은 **작게** 잡는다. 거리 하나로 다 정해지면 위협 관리가 무의미해지고,
## 반대로 도발이 너무 세면 다른 축이 다 죽는다. 도발(+35)이 4칸 거리를 뒤집는
## 정도가 지금 균형이다.

const BASE: int = 10
const PER_TILE: int = -2       ## 가까울수록 위협
const RECENT_HIT: int = 6      ## 직전 틱에 그 적을 때렸다

## ── 다친 것은 위협이 아니다 ─────────────────────────────────────────────
## HP 30% 이하면 +4 를 주고 있었다. "마무리하러 온다" 는 뜻이었는데, 그러면
## **내가 아무것도 안 했는데 어그로가 끌린다.**
##
## 위협도는 지휘관이 설계하는 값이어야 한다. 앞에 서고, 때리고, 도발하는 것은
## 전부 내가 고른 행동이다. 그런데 "맞아서 피가 없다" 는 내가 고른 것이 아니고,
## 그 결과로 더 맞으면 한 번 밀리기 시작한 판은 되돌릴 방법이 없다.
##
## 뺐다. 남은 항목은 전부 **한 일**이다 - 거리(어디에 섰나) · 최근 타격
## (누구를 쳤나) · 누적 피해/회복(얼마나 일했나) · 태세(무엇을 선언했나).

## 태세가 붙이는 보정. core/rules.gd 가 unit.threat_mod 에 채운다.
const TAUNT: int = 35
const AGGRESSIVE: int = 20
const STEALTH: int = -15


## 한 일이 쌓여 만드는 위협. 누적 피해·회복의 이 비율만큼이 위협이 된다.
##
## 이게 없으면 위협은 거리 하나로만 정해지고, 딜을 아무리 넣어도 어그로가 안
## 끌린다. "탱커가 관리 안 하면 딜러가 어그로를 뺏긴다" 는 감각이 여기서 나온다.
##
## ── 100 은 아무 일도 안 하는 값이었다 ───────────────────────────────────
## 한 판에서 대원이 쌓는 누적 피해·회복을 실측했다(표본 45).
##
##   중앙 165 · 75% 235 · 90% 330 · 최대 557
##
## 100 으로 나누면 중앙 +1, 최대 +5 다. 거리 한 칸이 2점이니 **딜을 최대로
## 넣어도 두 칸 반 앞으로 나온 것만 못하다.** 주석에 적힌 "딜러가 어그로를
## 뺏긴다" 는 감각이 코드에는 없었다.
##
## 30 으로 내렸다.
##   중앙 +5   앞으로 두세 칸 나온 것과 같은 무게
##   90%  +11  거리 차 다섯 칸을 뒤집는다
##   최대 +18  Rules.THREAT_SOFT(18)에 딱 닿는다 - 정말 많이 때린 딜러는
##             "코앞의 위협" 과 같은 급이 되어, 원거리 대원이 그를 등지지 않는다
##
## 도발(+35)은 못 넘는다. 일해서 끌리는 어그로가 선언해서 끄는 어그로를
## 이기면 위협 관리가 성립하지 않는다.
const PER_WORK: int = 30

## 표적을 바꾸는 데 필요한 초과분(%).
##
## ── 왜 마진이 필요한가 ───────────────────────────────────────────────────
## 1이라도 높으면 바로 갈아타면, 두 대원의 위협이 엎치락뒤치락하는 동안 적이
## 매 틱 표적을 바꾸며 제자리에서 떤다. 화면에서는 아무 일도 안 일어나고
## 플레이어는 왜 그러는지 알 수 없다.
##
## 25% 를 넘어야 갈아탄다. 그래서 도발은 "확실히 크게" 끌어야 의미가 있고,
## 딜러가 어그로를 뺏으려면 정말 많이 때려야 한다.
const SWITCH_MARGIN: int = 125


## chooser 가 볼 때 candidate 가 얼마나 위협적인가.
static func score(chooser: Unit, candidate: Unit) -> int:
	# ── 잠복 중이면 위협도가 없다 ────────────────────────────────────────
	# 표적 후보에서는 이미 빠져 있었다(Battle.living_enemies_of). 그런데
	# 위협도는 그대로 계산돼서, 전황판의 위협 막대와 어그로 네모에는 숨은
	# 대원이 여전히 **가장 위협적인 대원**으로 떠 있었다.
	#
	# 화면이 "이 대원이 표적이다" 라고 말하는데 엔진은 그 대원을 못 고르는
	# 상태였던 것이다. 어느 쪽이 맞느냐가 아니라 둘이 다르다는 것이 문제다.
	if candidate.ambush_ticks > 0:
		return 0
	var n := BASE
	n += Grid.manhattan(chooser.pos, candidate.pos) * PER_TILE
	if candidate.last_target != null and candidate.last_target.index == chooser.index:
		n += RECENT_HIT
	n += candidate.threat_mod
	# 개체 특성이 상시로 얹는 값. 유인 신호기가 여기로 들어온다.
	n += candidate.threat_base
	# 한 일이 쌓인 만큼. 딜을 많이 넣은 대원이 결국 맞게 된다.
	n += (candidate.damage_dealt + candidate.healing_done) / PER_WORK
	return n
