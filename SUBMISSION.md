# 제출 체크리스트

1인 참여 → **5번(팀원 롤 기술서)은 생략.** 나머지 4개는 하나라도 빠지면 심사 제외.

| # | 항목 | 형태 | 상태 | 남은 일 |
|---|---|---|---|---|
| 1 | 플레이 가능한 빌드 + 전체 소스 | GitHub Pages 링크 | **미완** | git 저장소 생성 · Pages 배포 |
| 2 | 플레이 동영상 30~60초 | YouTube 링크 | **미완** | 녹화 · 업로드 |
| 3 | 게임 소개 및 설명 | PDF | **초안 있음** (아래 §3) | 링크 채우고 PDF 변환 |
| 4 | AI 활용 기술 문서 | PDF | **초안 있음** (아래 §4) | 프롬프트 내역 보강 · PDF 변환 |
| 5 | 팀원 롤 기술서 | PDF | **생략** | — |

---

## 1. 빌드 + 소스 (제일 급함)

### 지금 상태

- 웹 빌드는 `web/` 에 있고 로컬에서 정상 동작 확인됨 (42MB — wasm 38MB + pck 3.2MB)
- **git 저장소가 없다.** 요구사항이 *"게임 전체 소스 코드를 동일 저장소에 포함하고,
  커밋 기록을 유지해 주세요"* 라서 이게 1번 블로커다.

### 해야 할 일

```
1) git init + 첫 커밋
2) web/ → docs/ 로 이름 변경
   GitHub Pages 는 브랜치 루트나 /docs 만 서빙한다. web/ 은 못 쓴다.
3) GitHub 저장소 생성 후 push
4) Settings → Pages → Source: main branch / docs 폴더
5) 공개(public) 권장. 비공개면 dl_gameai_reviewer@nhn.com 초대
```

### 주의

- **`.gitignore` 에서 `*.import` 를 빼야 한다.** Godot 4 의 `.import` 파일은
  리소스 UID 를 들고 있어서, 빠진 채로 클론하면 씬의 `uid://` 참조가 어긋날 수 있다.
  `.godot/` 만 무시하는 게 맞다.
- 웹 빌드는 `thread_support=false` 로 뽑았다. SharedArrayBuffer 를 안 쓰므로
  GitHub Pages 처럼 COOP/COEP 헤더를 못 주는 정적 호스팅에서도 돌아간다.
  **이 설정을 켜면 Pages 에서 흰 화면이 된다.**
- 심사 종료까지 링크가 살아 있어야 한다.

---

## 2. 플레이 동영상 (30~60초)

AI 합성·타인 영상 도용 불가. **실제 플레이 화면 그대로.**

### 촬영 구성안 (55초)

| 구간 | 내용 | 노림수 |
|---|---|---|
| 0-8초 | 타이틀 → 상점. 카드 두 장 구매 | 무엇을 사는 게임인지 |
| 8-20초 | 편성. 유닛 배치 → 카드를 슬롯 1·2에 꽂기 | **순서를 정하는 장면** |
| 20-35초 | 전투 2배속. 머리 위 규칙 라벨이 뜨는 것 | 카드가 그대로 실행되는 게 보임 |
| 35-45초 | 편성으로 돌아가 **슬롯 1·2를 바꾸고** 재전투 | 같은 카드, 다른 결과 — 이 게임의 전부 |
| 45-55초 | 승리 → 보상 화면(강화 선택) | 로그라이트 진행 |

**35-45초 구간이 핵심이다.** 이 게임의 유일한 주장이 "순서가 곧 전술"인데,
그걸 보여주는 방법은 같은 카드로 두 번 싸우는 것뿐이다.

전투는 결정론(난수 0)이라 **같은 편성은 항상 같은 전개**다. 리허설한 대로 찍힌다.

---

## 3. 게임 소개 및 설명 문서 — 초안

> 아래를 그대로 PDF 로 옮기면 된다. 링크 두 개만 채우면 완성.

### GAMBIT GRID

**내가 조종하는 건 캐릭터가 아니라 캐릭터의 사고방식이다.**

규칙을 짜서 AI를 싸우게 하는 격자 오토배틀러.

### 게임 방법

**목표** — 5개 스테이지를 연속으로 돌파한다. 하나라도 지면 런이 끝난다.

**조작** — 전투 중에는 아무것도 조작하지 않는다. 전투 전에 하는 일이 전부다.

1. **덱 구성** — 예산으로 규칙 카드를 산다. 카드 한 장 = `조건 → 행동` 한 문장.
   예) `적이 2칸 이내 → 적 반대로 1칸`
2. **편성과 배치** — 유닛 3명을 격자에 세우고, 각자에게 카드를 최대 3장 꽂는다.
3. **전투** — 매 틱 유닛은 **자기 카드를 위에서부터 읽어 조건이 맞고 실행도 되는
   첫 번째 것 하나**만 실행한다. 안 맞으면 다음 칸, 다 안 맞으면 직업 기본기.

**핵심** — 카드를 꽂는 **순서**가 전략이다. `교전`을 1번에 두면 사거리에 들어오는
순간 쏘고, `거리 유지`를 1번에 두면 먼저 물러난다. 같은 두 장으로 완전히 다른
대원이 된다.

**종료 조건**
- 승리 — 적 전멸
- 패배 — 아군 전멸, 또는 **14틱 동안 아무 피해도 없으면**(정체) 패배
- 5스테이지까지 깨면 런 클리어

**진행** — 스테이지를 깨면 셋 중 하나를 고른다: 유닛 강화 / 희귀 카드 / 예산.
카드는 같은 것 2장을 합쳐 단계를 올릴 수 있다(최대 3단계).

### 실행 방법

브라우저에서 링크를 열면 바로 실행된다. 설치·플러그인 없음.

- 플레이 링크: `(GitHub Pages URL)`
- 플레이 영상: `(YouTube URL)`
- 권장 환경: 데스크톱 크롬/엣지, 1280×720 이상
- 처음 로딩에 wasm 38MB 를 받는다. 회선에 따라 10~30초 걸릴 수 있다.

### 콘텐츠 규모

유닛 6종 · 전술 카드 18종 · 궁극기 6종 · 스테이지 5개 · 튜토리얼 27단계

---

## 4. AI 활용 기술 문서 — 초안

### 4-1. 게임 안의 AI (설계)

이 게임의 AI는 **머신러닝이 아니라 규칙 기반 결정 엔진**이다. 플레이어가 짠
규칙을 유닛이 해석해 행동한다.

**평가 순서** (`core/rules.gd`)

```
궁극기(우선 토글 시) → 슬롯 1 → 2 → 3 → 궁극기(전술 먼저 시) → 직업 기본기 → 공통 골격
```

**폴스루(fall-through)가 핵심이다.** 조건이 참이어도 그 행동이 실제로 불가능하면
(대상이 없거나, 사거리 밖이거나, 이동할 칸이 없으면) 다음 슬롯으로 내려간다.
이게 없으면 `돌격 → 교전`(조건이 `항상`)에서 2번 카드가 영영 실행되지 않는다.

**어휘** — 조건 15종 · 행동 8종 · 대상 10종의 조합으로 카드를 정의한다.
데이터(`data/cards.gd`)만 고치면 새 카드가 생기고, 엔진은 안 바뀐다.

**완전 결정론** — 전투에 난수가 0개다. 행동 순서는 유닛 index, 모든 동점은
index 와 방향 상수 순서로 끊고, HP 계산은 정수 연산만 쓴다. 같은 편성은 항상 같은
전개가 나온다. 덕분에 (a) 영상 재촬영이 가능하고 (b) 밸런스를 시뮬레이션으로 잴 수
있다.

### 4-2. 개발에 쓴 AI 도구

| 도구 | 용도 |
|---|---|
| Claude (Anthropic) | 설계 상담 · 코드 작성 · 밸런스 시뮬레이터 제작 및 해석 |
| DeepSeek (deepseek-v4-flash) | 정적 데이터 테이블 일부 초안 |

> **채워야 할 것**: 실제로 준 주요 지시문 5~10개를 옮겨 적을 것.
> 심사 항목이 *"AI 대상 주요 프롬프트 및 지시 사항"* 이다.

### 4-3. AI 협업에서 실제로 겪은 것 (문서에 넣으면 좋은 내용)

- **DeepSeek 위임** — 기계적 필드는 정확했으나 한국어 고유명사를 임의로 바꿔
  전부 수기 복원. 이후 데이터 테이블만 위임하고 판단이 필요한 코드는 위임하지 않음.
- **검사 체계를 7종으로 늘린 이유** — AI가 만든 코드에서 반복해 나온 결함이
  "데이터는 맞는데 배선이 끊긴" 유형이었다. 오류도 안 나고 화면도 멀쩡해서
  눈으로는 못 잡는다. 그래서 *선언한 값과 실제 동작을 대조하는* 검사를 만들었다.
  (예: 궁극기 등장 확률 상수 vs 실측 등장률)
- **밸런스는 감이 아니라 측정으로** — 런 단위 몬테카를로(`test/runsim.gd`)로
  스테이지별 승률을 재고, 1스테이지 승률 16.9% → 80.6% 로 교정했다.

### 4-4. 외부 에셋 · 오픈소스 출처

`CREDITS.md` 참조. 요약:

- Godot Engine 4.7.1 — MIT
- 던파 비트비트체 / v2 — OFL 1.1
- Pretendard — OFL 1.1
- 효과음 7종 — 제작자 직접 제작
- 효과음 6종 — 코드가 실행 시점에 합성(외부 저작물 아님)
- 아트 — 없음 (전부 코드로 그린 도형)
