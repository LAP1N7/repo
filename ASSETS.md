# 에셋 제작 스펙

> ## 현재 상태 (수시 갱신)
>
> | 종류 | 상태 |
> |---|---|
> | **폰트** | **완료** — 던파 비트비트체 + v2 (크기별 자동 선택). 추가 불필요 |
> | **효과음** | **7종 완료** (직접 제작) + 6종은 코드 합성. 교체 규격은 `assets/sfx/README.md` |
> | 유닛 스프라이트 6종 | 미착수 — **체감 차이가 제일 큰 항목** |
> | 튜토리얼 초상 (궁수) | 미착수 — 이거 1장이면 튜토리얼 전체가 돈다 |
> | 궁극기 컷인 6종 | 미착수 |
> | 카드 배너 18종 | 미착수 — 없어도 카드는 읽힌다. 제일 마지막 |
>
> **폰트가 바뀌었다.** 아래 본문에 "프리텐다드" 로 적힌 곳이 있는데, 지금은
> 던파 비트비트체가 주 폰트이고 프리텐다드는 폴백이다. 픽셀 계열이지만
> **아트 방향은 여전히 고해상도 2D 로 간다** — 폰트만 픽셀이고 렌더는 Linear 필터다.
>
> 세계관상으로도 근거가 생겼다. 화면은 "가공 전 시뮬레이션 레이어" 이고 해석은
> 플레이어의 뇌가 붙인다는 설정이라, 도형 렌더가 변명이 아니라 의도가 된다.
> **컷인(궁극기)만 "해석이 붙은 순간"** 으로 연출하면 그림이 왜 그 순간에만
> 나오는지까지 설명된다. (`TODO.md` B-3 참조)


게임 해상도 **1280 × 720** 고정, 타일 **64 × 64**.
"표시" = 게임 안에서 실제로 그려지는 크기. "제작" = 넘겨줄 원본 크기.

**제작 크기를 표시의 3~4배로 잡는 이유:** 고해상도 모니터에서 창을 키우면 Godot 이
화면 전체를 확대한다. 원본이 표시 크기와 같으면 그때 뭉개진다. 8배씩 키우면 wasm
용량만 늘고 이득이 없다.

**넣기만 하면 자동으로 붙는다.** 코드는 `ResourceLoader.exists()` 로 파일을 확인하고
있으면 얹고 없으면 도형으로 남는다. 한 장씩 들어와도 게임이 안 깨진다.

---

## 0. 픽셀아트 vs 고해상도 2D → **고해상도 2D**

앞서와 같은 결론이고, 네가 준다는 목록을 보니 더 확실해졌다.

1. **UI 가 고해상도 텍스트 중심이다.** 카드에 긴 한글 문장이 10~14px 로 들어가고
   프리텐다드로 선명하게 렌더된다. 픽셀 유닛이 붙으면 해상도가 따로 논다.
2. **네가 주겠다는 "특수 스킬 컷인 일러스트" 는 태생적으로 고해상도다.**
   컷인만 고해상도이고 격자 위 유닛은 픽셀이면 같은 캐릭터가 두 화풍으로 보인다.
3. **트위닝(부드러운 보간)을 쓸 거면 픽셀아트와 상극이다.** 픽셀아트는 정수 좌표에
   딱딱 떨어져야 격자가 유지되는데, 이 게임은 이동·확대·회전을 전부 보간한다.
   지금 카드 호버도 1.07배 확대 + 미세 회전이라 픽셀이 뭉개진다.
4. 애니메이션 프레임을 많이 찍을 계획이 아니라면 픽셀아트의 최대 강점이 안 나온다.

`project.godot` 의 `default_texture_filter` 는 **1(Linear)** 로 맞춰 두었다.
픽셀로 뒤집을 거면 **0(Nearest)** 으로 되돌리고 정수배 확대만 써야 한다.

---

## 1. 레이어 규칙 — 이게 제일 중요하다

**한 장에 다 그리지 말 것.** 아래 요소는 코드가 따로 그리므로, 그 부분을 그림에
포함하면 두 번 그려져서 어긋난다.

### 격자 위 유닛의 실제 합성 순서 (아래 → 위)

```
  ① 그림자          ← 코드가 그린다 (타원)      ✗ 그림에 넣지 말 것
  ② 팀 링           ← 코드가 그린다 (발밑 고리)  ✗ 그림에 넣지 말 것
  ③ 본체            ← 여기가 네 PNG              ✓ 이것만 그린다
  ④ 방어 태세 링     ← 코드                      ✗
  ⑤ HP 바 · 쿨다운   ← 코드                      ✗
  ⑥ 이름            ← 코드 (한글 텍스트)         ✗
  ⑦ 규칙 라벨 칩     ← 코드 (항상 최상단)         ✗
```

즉 **유닛 PNG 에는 캐릭터 몸통만** 있으면 된다. 그림자·테두리·HP바·이름을 그려 넣으면
코드가 그리는 것과 겹친다.

**피격 시 코드가 스프라이트 전체를 하얗게 태운다**(modulate). 그래서:
- 캐릭터가 **완전한 흰색**이면 피격이 안 보인다. 순백은 피할 것
- 반투명 픽셀은 플래시 때 지저분해진다. 외곽은 **깔끔하게 자를 것**

### 카드의 합성 순서

```
  ① 카드 배경판      ← 코드 (코스트 색 테두리)
  ② 일러스트 배너    ← 여기가 네 PNG          ✓
  ③ 하단 그라데이션  ← 코드 (글자 가독성용)
  ④ 코스트 배지      ← 코드 (원 + 숫자)
  ⑤ 이름 · 조건 · 행동 ← 코드 (한글 텍스트)
```

**배너에 글자를 넣지 말 것.** 카드 이름과 규칙 문장은 코드가 얹는다.
배너 아래쪽 55% 는 코드가 어둡게 덮으므로 **중요한 그림은 위쪽 45% 에** 배치할 것.

### 컷인의 합성 순서

```
  ① 암막            ← 코드 (검정 82%)
  ② 색 띠           ← 코드 (유닛 색)
  ③ 캐릭터 일러스트  ← 여기가 네 PNG          ✓
  ④ 스킬 이름       ← 코드 (52px 흰 글자, 화면 중앙)
  ⑤ 전체 플래시      ← 코드 (흰색)
```

일러스트는 **화면 오른쪽 절반(x 600~1280)** 에 들어온다.
스킬 이름이 화면 **가로 중앙 y=300 부근** 에 크게 얹히므로, 얼굴을 거기 두면 가린다.
→ **얼굴은 화면 위쪽 1/3 또는 오른쪽으로** 배치할 것.

---

## 🔴 1순위

### A. 유닛 스프라이트 (6종) — 가장 급함

| 항목 | 값 |
|---|---|
| 표시 | **가로 48 고정.** 세로는 원본 비율대로 따라온다 |
| 제작 | **권장 192 × 240 (1 : 1.25)** — SD 체형에 맞춰 실측한 값 |
| 허용 범위 | 192 × 192 (1:1) ~ **192 × 256 (1:1.33)** |
| 절대 최대 | 192 × 256. 그 이상은 위 칸을 통째로 덮는다 |
| 배경 | 투명 |
| 방향 | **오른쪽을 보게.** 적은 코드가 좌우 반전한다 |
| 기준선 | **발끝을 캔버스 맨 아래에 붙일 것.** 코드가 발밑을 고정해 세운다 |
| 금지 | 그림자, 테두리, HP바, 이름, 순백 |

**세로 비율에 대해 (SD 체형 확인 완료).**
스프라이트는 **타일 중심이 아니라 발밑 기준**으로 선다. 그래서 키가 커져도 아래로
밀리지 않고 위로만 자란다. 세로 길이별 실제 화면 높이:

| 제작 비율 | 화면 높이 | 위 칸 침범 | 판정 |
|---|---|---|---|
| 192 × 192 (1:1) | 48px | 없음 | 안전하지만 SD 느낌은 약함 |
| **192 × 240 (1:1.25)** | **60px** | 살짝 | **권장 — 실측으로 가장 좋았다** |
| 192 × 256 (1:1.33) | 64px | 위 칸 바닥까지 | 최대치 |
| 192 × 288 (1:1.5) | 72px | 위 칸을 넘어섬 | ✗ 세로로 붙은 유닛이 한 덩어리로 뭉친다 |

위 칸을 조금 침범하는 건 정상이다. **Y 정렬**을 켜 두어 화면 아래쪽 유닛이 항상
앞에 그려지고, **HP 바·쿨다운은 별도 레이어(z 60)** 로 모든 스프라이트 위에 떠서
앞줄이 뒷줄의 체력을 가리지 않는다.

**아트가 들어가면 유닛 이름표는 자동으로 사라진다.** 세로로 긴 스프라이트 위에
이름까지 얹으면 위 유닛의 몸을 가리고 글자끼리 겹쳐 아무것도 안 읽힌다.
생김새로 구분되면 이름은 불필요하다. (도형 상태에서만 이름이 나온다)

```
assets/art/units/warrior.png     전사   HP125 공18 사거리1 이동1  남캐 · 도끼 버서커. 몰릴수록 세진다
assets/art/units/archer.png      궁수   HP65  공24 사거리3 이동1  여캐 · 기계식 활 (튜토리얼 주인공)
assets/art/units/bard.png        악사   HP72  공9  사거리2 이동1  성별 미상 · 여러 악기를 다룬다
assets/art/units/assassin.png    암살자 HP58  공32 사거리1 이동2  남캐 · 쌍단검
assets/art/units/musketeer.png   총사   HP100 공20 사거리2 이동1  여캐 · 산탄총. 인파이팅 원딜
assets/art/units/shieldman.png   방패병 HP190 공9  사거리1 이동1  방패에 싸여 얼굴이 보이지 않는다
```

> **실루엣이 역할을 말하게.** 48px 에서는 디테일이 안 보인다.
> 방패병은 넓고 낮게, 암살자는 좁고 날카롭게, 궁수는 세로로 길게.

**트위닝(애니메이션)에 대해:** 지금 코드는 정지 이미지 + 위치 보간만 쓴다.
프레임 애니메이션을 넣을 거면 **가로 스트립**(프레임을 가로로 이어붙인 한 장)으로
주면 된다. 다만 **우선순위는 낮다** — 정지 이미지 6종이 먼저다.
줄 거면: `warrior_idle.png` 가로 4프레임 = 768 × 192, 초당 6프레임 루프.

### B. 카드 일러스트 (전술 13 + 특수 6 = 19종)

| 항목 | 값 |
|---|---|
| 표시 | **128 × 64** (가로 긴 띠) |
| 제작 | **384 × 192** |
| 배경 | **불투명** |
| 구도 | 상황 한 컷. 인물 얼굴보다 **행동**이 읽히게 |
| 금지 | 글자, 카드 테두리 |
| 주의 | **아래 55% 는 어둡게 덮인다.** 중요한 그림은 위쪽에 |

파일명은 카드 id 그대로:
```
전술: engage, finisher, snipe, charge, retreat, resolve, keep_distance,
      counter, evade, guard_stance, mend, hold_ground, pursue
특수: volley_retreat, cleave, pierce_shot, blessing, blink_strike, bulwark
→ assets/art/cards/engage.png ...
```

> **여유 없으면 행동 종류별 공용 6장만** 먼저: 공격 / 접근 / 후퇴 / 방어 / 회복 / 대기.
> 그걸 해당 카드들에 복사해 쓰면 된다.

---

## 🟡 2순위

### C. 특수 스킬 컷인 일러스트

| 항목 | 값 |
|---|---|
| 표시 | 가로 720 영역에 비율 유지로 맞춤 (화면 오른쪽 절반) |
| 제작 | **1080 × 1440** (세로 긴 판형) |
| 배경 | 투명 |
| 구도 | 반신~전신. **왼쪽(화면 안쪽)을 보게** |
| 주의 | 화면 중앙 y=300 에 스킬 이름이 52px 로 얹힌다 → 얼굴을 그 높이에 두지 말 것 |

파일명은 **유닛 id**:  `assets/art/cutin/musketeer.png`, `archer.png`, …

컷인은 이제 **궁극기 6종 전부**에 붙는다. 전투당 1회뿐이라 매번 특별한 순간이고,
그래서 6장 다 필요하다 (없으면 컷인 없이 그냥 발동한다 — 게임은 돌아간다).

| 유닛 id | 궁극기 | 그림 방향 |
|---|---|---|
| `musketeer` | 거리두기 | 산탄을 갈기며 뒤로 빠지는 순간 |
| `archer` | 집중사격 | 숨을 멈추고 조준선을 좁히는 순간 |
| `assassin` | 비영천참 | 잔상만 남기고 사라진 직후 |
| `warrior` | 불굴의 의지 | 쓰러지지 않고 버티는, 피투성이 |
| `shieldman` | 최후의 수호 | 아군 앞을 가로막고 방패를 세운 순간 |
| `bard` | 칸타빌레 | 악기를 크게 울리는, 빛이 퍼지는 |
**이 3장만 먼저** 만들면 된다.

### C-2. 튜토리얼 초상 (전신 LD) — **컷인과 별개다**

튜토리얼 대사 옆에 서는 전신 캐릭터. 컷인은 "한순간 터지는" 그림이고 이건
**대사 내내 화면에 서 있는** 그림이라 요구가 다르다.

| 항목 | 값 |
|---|---|
| 표시 | 세로 720 에 맞춤, 가로 420 영역 안 |
| 제작 | **840 × 1440** (세로 2배) |
| 배경 | 투명 |
| 구도 | **전신, 서 있는 자세.** 발끝이 캔버스 하단에 붙게 |
| 시선 | 화면 안쪽(말풍선 쪽)을 보게. 좌우 배치는 대본이 정하고 코드가 뒤집는다 |
| 표정 | 무표정~담담. 이 캐릭터는 필요한 말만 하는 성격이다 |
| 금지 | 말풍선, 글자, 배경, 강한 원근(위아래가 잘린다) |

`assets/art/tutorial/<유닛 id>.png` — 지금은 **궁수 1장**(`archer.png`)이면 튜토리얼 전체가 돈다.

> 튜토리얼 화자를 총사에서 궁수로 바꿨다. 총사는 사거리 2 라 `거리 유지`(2칸 이내
> → 후퇴) 문턱과 값이 같아서 쏠 수 있는 거리 전부에서 물러난다 — 순서를 가르치는
> 튜토리얼에서 바로 그 카드를 못 쓴다. 궁수는 사거리 3 이라 "3칸에서 쏘고 2칸이면
> 물러난다" 는 밴드가 생겨서 교재로 쓸 수 있다.
없으면 회색 실루엣으로 대체되므로 게임은 그대로 돌아간다.

> 컷인(`assets/art/cutin/`)을 임시로 돌려쓰고 있다. 전신 LD 가 들어오면 그쪽이
> 우선 적용되게 되어 있다.

### C-3. 말풍선 프레임 (9-slice)

| 항목 | 값 |
|---|---|
| 제작 | **144 × 144** (모서리 36px 고정, 가운데가 늘어남) |
| 배경 | 반투명 가능 |
| 내용 | 테두리와 배경만. **꼬리(말풍선 뾰족한 부분)는 넣지 말 것** |

꼬리를 넣으면 안 되는 이유: 말풍선이 가리키는 대상이 화면 아무 데나 올 수 있어서
(카드·버튼·격자) 코드가 **아래·위·왼쪽·오른쪽 중 겹치지 않는 자리**로 옮긴다.
꼬리 방향이 고정되면 엉뚱한 곳을 가리키게 된다.

`assets/art/ui/bubble.png`

### C-4. 진행 버튼 / 앵커 표시

| 종류 | 제작 | 비고 |
|---|---|---|
| `계속` 버튼 (평상/호버/눌림) | 각 **240 × 60** | 9-slice, 모서리 20px |
| 앵커 하이라이트 테두리 | **96 × 96** | 9-slice, 모서리 24px. 가운데는 **완전 투명** |

앵커 테두리 가운데가 투명해야 하는 이유: 눌러야 할 UI 위에 씌우는 틀이라
가운데가 채워져 있으면 정작 그 버튼이 안 보인다. 지금은 코드가 맥박치는 사각형을
그리고 있고, 이미지가 들어오면 그걸로 교체된다.

`assets/art/ui/btn_continue_{normal,hover,pressed}.png`, `assets/art/ui/anchor.png`

### D. 유닛 초상 (편성 화면)

| 항목 | 값 |
|---|---|
| 표시 | **40 × 40** |
| 제작 | **160 × 160** |
| 배경 | 투명 |
| 구도 | 얼굴 클로즈업, 정면 |

`assets/art/portraits/<유닛 id>.png`

---

## 🟢 3순위 — 완성도를 올리는 것들

### E. 격자 위 엔티티 — **보류 결정 (2026-07-31)**

넣지 않기로 했다. 다만 이유를 정확히 남긴다.

**알고리즘 비용은 거의 없다.** `Grid.step_toward` 는 자유 칸만 따라 퍼지는 BFS
거리장이고(그리디 대신 BFS 를 고른 이유가 이것이다), `Grid.has_line_of_sight` 도
차단 집합을 인자로 받는다. 둘 다 장애물 좌표를 그 집합에 넣기만 하면 된다.

**진짜 비용은 밸런스와 예외다:**
- 막다른 곳에 갇힌 유닛이 `idle` 로 떨어진다. 타임아웃이 이미 22~45% 인데 더 는다
- 스테이지 5개를 전부 다시 잡아야 한다 — 지형이 생기면 기존 수치가 무의미해진다
- 적도 같은 규칙 엔진이라 "바위를 끼고 싸운다" 같은 판단을 못 한다.
  플레이어만 일방적으로 이득 보는 구조가 되기 쉽다

밸런스가 안정된 뒤에 붙이면 싸다. 그때까지 아트도 만들지 말 것.

<details><summary>나중에 되살릴 때의 규격</summary>

**아직 게임에 없는 기능이다.** 넣으면 차폐(LOS)가 훨씬 의미 있어지고 스테이지마다
지형이 달라져서 배치 고민이 커진다. 넣을 거면 아트보다 **규칙을 먼저 정해야** 한다.

| 종류 | 규칙 제안 | 표시 | 제작 |
|---|---|---|---|
| 바위 / 잔해 | 이동 불가 + **사선 차단** | 56 × 56 | 224 × 224 |
| 덤불 | 이동 가능 + 사선 차단 | 56 × 56 | 224 × 224 |
| 구덩이 | 이동 불가, 사선은 통과 | 56 × 56 | 224 × 224 |

`assets/art/props/rock.png` 등. 배경 투명, 그림자 넣지 말 것(코드가 그린다).
규칙·데이터·스테이지 배치를 먼저 만든 뒤에 아트를 받는 순서로 갈 것.

</details>

### F. 타일 · 배경

| 종류 | 표시 | 제작 | 비고 |
|---|---|---|---|
| 타일 A/B (체크무늬 두 장) | 64 × 64 | 256 × 256 | **이음매 없이(seamless)** |
| 진영 표시 오버레이 | 64 × 64 | 256 × 256 | 반투명. 아군/적 2장 |
| 전투 배경 | 1280 × 720 | 2560 × 1440 | 격자 뒤. 어둡고 저채도로 |
| 로비 배경 | 1280 × 720 | 2560 × 1440 | 타이틀 화면 |

### G. 아이콘 (24 × 24 표시 / 96 × 96 제작, 투명)

카드와 규칙 목록에서 조건·행동을 한눈에 구분시킨다.
```
조건: always, in_range, out_of_range, hp_low, was_hit, surrounded, ally_hp_low
행동: attack, move_toward, move_away, defend, hold, heal
```
`assets/art/icons/attack.png` 등. **단색 실루엣**으로 만들면 코드가 색을 입힌다.

### H. UI 프레임 (9-slice)

| 종류 | 제작 | 비고 |
|---|---|---|
| 패널 프레임 | 96 × 96 | 모서리 24px 씩 고정, 가운데 늘어남 |
| 버튼 (평상/호버/눌림) | 96 × 48 × 3장 | 위와 동일 |
| 카드 뒷면 틀 | 384 × 504 | 카드 전체 테두리를 아트로 교체할 때 |

> 9-slice 는 **모서리 크기를 알려줘야** 한다. 24px 기준으로 만들어 주면 코드에서
> 그대로 설정한다.

### I. 로딩 화면 (웹 배포용 — DESIGN R1 "필수")

wasm 이 39MB 라 첫 로딩이 10~20초 걸린다. 흰 화면이면 심사자가 닫는다.

| 항목 | 제작 | 비고 |
|---|---|---|
| 로딩 배경 | 1920 × 1080 | 진행 바는 코드가 그린다 |
| 로고 | 가로 1200 이상, 투명 | 제목 확정 후 |

### J. 사운드 (제목·컨셉 확정 후)

지금은 보류. 필요해지면 목록을 따로 뽑아 줄게. 기본 방침은 **kenney.nl(CC0)** 이고,
쓰는 순간 `CREDITS.md` 에 기록한다.

---

## 우선순위 요약

네가 지금 당장 만들면 되는 것, 순서대로:

1. **유닛 6종** (192×192, 투명, 오른쪽 보게, 몸통만) ← 이거 하나만 들어와도 인상이 바뀐다
2. **컷인 6종** (1080×1440, 투명, 왼쪽 보게 — 궁극기 하나당 한 장)
3. **카드 아트 6장 공용** (384×192, 불투명, 위쪽 45% 에 주요 그림)
4. 초상 6종 → 배경 2장 → 아이콘 → 9-slice 프레임

**기초 스토리**는 아트가 아니라 텍스트라 언제 줘도 좋다. 받으면 게임 방법 화면과
스테이지 시작 문구에 녹일 수 있게 구조를 잡아 둘게.
