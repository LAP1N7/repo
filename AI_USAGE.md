# AI 활용 기술 문서

**PROJECT RECLAIM (프로젝트 리클레임)**
Godot 4.7.1 · GDScript · 웹(WebAssembly) 빌드

플레이: https://lap1n7.github.io/repo/
소스: https://github.com/LAP1N7/repo

---

## 0. 이 문서를 쓴 원칙

세 가지만 지켰다.

- **실제로 쓴 것만 적는다.** 써 보고 안 쓴 도구는 적지 않았다.
- **추측해서 채우지 않는다.** 확인되지 않은 항목은 확인될 때까지 비워 뒀다.
- **과장하지 않는다.** 수치는 저장소에서 직접 센 값이다.

---

## 1. 프로젝트 개요

**"지휘관이 조종하는 것은 대원이 아니라 대원의 사고방식입니다."**

전투 중에는 아무것도 조작하지 않는다. 전투 **전에** 대원의 판단 규칙을 짜고,
전투가 시작되면 그 규칙이 스스로 돌아가는 것을 지켜본다. 진 판을 보고 고치는
것은 손이 아니라 **알고리즘**이다.

| 항목 | 규모 |
|---|---|
| 전술 모듈 | 55종 (TARGET 16 · POSITION 12 · DOCTRINE 15 · PASSIVE 12) |
| 궁극기 | 6종 (직업 전용) |
| 대원 | 6종 (플레이어) + 기계 개체 6종 (적 전용) |
| 스테이지 | 5개 / 총 11 페이즈 |
| 판단 어휘 | 조건 13종 · 표적 선정 16종 · 자리 12종 · 태세 11종 · 상시 12종 |
| 보조 지휘 | 11항목 (3단계) |
| 대사 | 스토리 49줄 · 튜토리얼 35줄 · UI 문구 244개 |
| 코드 | GDScript 약 21,600줄 (그중 검증 코드 4,700줄) |
| 검증 | 검사 스크립트 23개 / 정기 스위트 9종 |

---

## 2. AI 활용 개요

AI를 **네 갈래로 나눠** 썼다. 도구를 많이 쓴 것이 아니라, **어떤 일을 어느
도구에 맡길지를 먼저 정한 것**이 이 프로젝트의 방식이다.

| 도구 | 맡긴 일 | 맡기지 않은 일 |
|---|---|---|
| **NovelAI** | 캐릭터·개체 일러스트 생성 | 구도 결정, 파츠 분리, 보정 |
| **Kling AI** | 타이틀 화면 루프 영상 | 화면 배치, 톤 조정 |
| **Claude** | 시스템 구조 검토, 코드 구현 보조, 밸런스 해석, 기획 정리 | 게임 기획, 세계관, 전투 규칙 결정 |
| **DeepSeek** | 명세가 확정된 반복 구현 | 아키텍처, 다중 파일 변경, 미확인 버그 |

**직접 수행한 것**: 게임 기획 · 전투 시스템 설계 · 캐릭터 컨셉 · 스토리와
세계관 · 밸런스 방향 · 코드 구현 · AI 생성 이미지 후처리.

---

## 3. NovelAI — 아트 에셋

캐릭터와 게임 내 기계 개체 일러스트를 생성했다. **생성이 작업의 시작이지
끝이 아니다.**

### 작업 절차

1. 프롬프트를 반복 수정하며 생성 (원하는 구도와 톤이 나올 때까지)
2. 결과물 선별
3. **Clip Studio Paint에서 직접 수정·보정**
4. **애니메이션용 파츠 분리** — 머리 / 몸통 / 팔 / 다리 / 무기
5. 분리 과정에서 가려져 있던 부분을 **직접 드로잉**

4·5번이 핵심이다. 생성 이미지는 한 장의 그림이라 관절이 없다. 팔을 떼어내면
그 아래에 아무것도 없으므로, 가려져 있던 몸통을 직접 그려 넣어야 움직인다.
현재 8종의 대원·개체가 파츠 리그(`.tscn`)로 움직이며, 걷기·공격 애니메이션이
각각 붙어 있다.

이미지 에셋은 82장이다(초상 · 스탠딩 · 스토리 배경 · 컷인 · SD 얼굴 · 리그 파츠).

### 대표 태그 (부록 D 참조)

`character illustration`, `sci-fi`, `anime style`, `white hair`, `blue eyes`,
`military uniform` 계열의 태그를 기반으로 반복 조정했다.

---

## 4. Kling AI — 타이틀 영상

메인 화면 배경으로 쓰는 루프 영상을 생성했다.

게임 내 타이틀 화면 하단에 `opening video · KlingAI 3.0` 으로 출처를 표기하고
있다. 실제 사용 프롬프트는 **부록 C**에 있다.

**영상을 받은 뒤 직접 한 작업**

- 워터마크가 박힌 하단을 잘라냄 (928×576, 1.611:1)
- Ogg Theora로 변환 (Godot이 재생하는 유일한 영상 코덱)
- 화면 배치: 가로를 1280에 맞춰 채우고, 글자가 앉는 왼쪽에 어두운 막을 덮음
- 불투명도 62% — 배경이지 그림이 아니다. 눈에 띄면 그 순간 실패다

---

## 5. Claude — 설계 검토 · 구현 보조

Claude는 **코드 작성 도구가 아니라 개발 보조 도구**로 썼다.

### 맡긴 일

- 전투 시스템 구조 검토
- 전술 모듈·캐릭터 능력 아이디어 정리와 역할 중복 진단
- 밸런스 논의 및 **측정 도구 제작**
- 코드 구현 방향 검토
- 기획 문서 정리

### 실제로 값을 한 지점 — 측정 도구

이 프로젝트에서 Claude가 가장 크게 기여한 것은 코드 줄 수가 아니라 **감으로
하던 밸런스를 측정으로 바꾼 것**이다.

전투에 난수가 하나도 없다(3-2절 참조). 그래서 "모듈 하나를 넣은 편성과 뺀
편성을 나란히 돌려 차이를 세는" 측정이 가능하다. 이 아이디어를 도구로 만든
것이 `test/module_lab.gd` 다.

```
                발동    변화    승    증감
회피 기동        223     650     2     -3
전투태세         463      56     4     -1
...
화력 우선        227     337    12     +7
```

- **발동** — 그 모듈이 이번 틱의 행동을 정한 횟수
- **변화** — 그 모듈이 없었을 때와 **다른 행동**을 한 횟수
- **증감** — 20판(편성 4 × 스테이지 5)에서의 승수 차

이 표가 드러낸 것: **DOCTRINE(교전 수칙) 축 15개 중 8개가 전부 마이너스이거나
0이었다.** 우연이 아니라 구조였다. 교전 축의 대다수가 "멈춘다 / 물러난다 /
막는다"라 그 틱에 딜을 안 넣는데, 이 게임에는 정체 패배와 틱 상한이 있다.
**시간이 자원인 게임에서 시간을 내고 산 생존이 딜로 환전되지 않으면 그냥
손해다.**

그 진단을 근거로 교전 축을 고쳤고, 고친 뒤 다시 재서 확인했다. 예:

| 모듈 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 광전 | 발동 14 / 변화 15 (표 전체 꼴찌) | 발동 20 / 변화 33 |
| 수비 태세 | -4 | **+3 (교전 축 최고)** |

수비 태세는 두 번 고쳤다. "제자리에서 방어"는 -4, "붙었을 때만 방어"도 -1,
**같은 조건에 위협 수칙(어그로)을 붙이자 +3**이 됐다. 위협 수칙은 행동 자리를
차지하지 않으므로 손해가 날 수가 없다는 것이 그때 드러났다.

### AI 협업에서 실제로 겪은 것

- **"데이터는 맞는데 배선이 끊긴" 결함이 반복됐다.** 오류도 안 나고 화면도
  멀쩡해서 눈으로는 못 잡는다. 그래서 *선언한 값과 실제 동작을 대조하는*
  검사를 따로 만들었다. 실제 사례: 튜토리얼 상점의 고정 카드 목록이
  `engage / keep_distance / pursue / hold_ground` 였는데, 축 시스템을 도입하며
  모듈 표를 갈아엎을 때 그 줄이 따라오지 않았다. **존재하지 않는 이름 다섯**
  이라 튜토리얼 상점이 통째로 비었고, 아무도 몰랐다. 지금은
  `test/tut_check.gd`(149검사)가 이런 단절을 잡는다.
- **화면은 코드로 검증되지 않는다.** 좌표만 보고 UI를 고치면 "이 요소는 맞는데
  옆 요소와 겹친다"를 절대 못 잡는다. 실제로 판 크기를 키우자마자 로그·하단
  바·버튼이 한꺼번에 겹쳤다. `test/shots.gd` 로 화면을 실제 렌더해 PNG로 남기고
  눈으로 확인하는 절차를 만들었다(현재 25장).

---

## 6. DeepSeek — 위임과 오케스트레이션

반복 구현의 효율을 높이기 위해 일부 작업을 DeepSeek(`deepseek-v4-flash`)에
위임했다. 다만 **"AI에게 코드를 시켰다"가 아니라 "언제·무엇을 위임할지를
규칙으로 정했다"**가 이 항목의 요지다.

### 위임 판단 기준

작업을 넘기기 전에 다음 여섯 가지를 **전부 적을 수 있어야** 넘겼다.

1. 정확한 파일 경로
2. 정확한 시그니처 / 구조
3. 모든 입력 갈래에 대한 기대 출력
4. **갈래마다 최소 하나의 구체적인 입력 → 출력 예시**
5. 명시적 제약 (표준 라이브러리만, 새 의존성 금지, I/O 금지)
6. **"건드리지 말 것" 목록**

그리고 두 원칙을 세웠다.

> **난이도가 기준이 아니다. 판단이 이미 끝났는가가 기준이다.**
>
> **명세를 쓰는 비용이 코드를 쓰는 비용보다 크면, 직접 쓴다.**

이 기준에 따라 **아키텍처 · 프레임워크 선택 · 다중 파일 변경 · 아직 원인을
파악하지 못한 버그**는 위임하지 않았다.

### 명세 길이의 효과 (실측)

같은 작업, 같은 모델, 프롬프트 길이만 바꿔 측정했다.

| 프롬프트 | 숨긴 테스트 통과 |
|---|---|
| 10단어 | 0/12 — 함수 이름을 제멋대로 바꾸고 자기 형식을 발명함 |
| 47단어 | 6/12 — 임포트는 되는데 **실제 입력 전부에 조용히 None 반환** |
| 194단어 | 12/12 |

가운데 줄이 함정이다. **반쪽 명세는 그럴듯하게 틀린 코드**를 만든다.
6/12 → 12/12로 올린 것은 모델도 길이도 아니라 **구체적인 입출력 예시**였다.

### 결과 검증

생성 결과를 그대로 쓰지 않았다. 실제로 반복해서 나온 실패 유형:

- **식별자 이름 변경** — `parse_line` 이 `parse_wand_serial_line` 으로 돌아옴.
  시그니처를 적어도 안 고쳐졌고, 예시를 넣어서야 잡혔다
- **범위 확대** — 요청하지 않은 로깅·타임스탬프·체크섬을 추가
- **설명은 맞는데 패치가 틀림** — 원인 분석 산문은 정확한데 결함은 그대로
- **한국어 고유명사 임의 변경** — 전부 수기 복원

**형식이 같은데 런타임 타입이 다른 경우**도 있었다. String 키를 요청했는데
GDScript의 `{name = "x"}` 문법으로 돌아왔고, 그건 **StringName** 키를 만든다.
컴파일되고 읽기에도 맞으며, Godot이 두 타입을 같은 키로 취급하기 때문에
우연히 동작했다. 값이 전부 맞아서 눈으로는 아무 문제가 없었다.

그래서 검토 후 **실제 검증기를 돌리는 것**을 절차에 넣었다. 위 결함 중 둘은
시각 검토를 통과했고 실행해 보고서야 잡혔다.

### 사용 결과

정적 데이터 테이블처럼 **정확히 맞는 것이 중요하고 검증이 싼** 파일에만
위임했다. 판단이 들어가는 코드(전투 상태 기계, 애니메이션, 아키텍처)는
위임하지 않았다.

---

## 7. AI 오케스트레이션 정리

| AI | 활용 목적 |
|---|---|
| NovelAI | 캐릭터 및 그래픽 에셋 생성 |
| Kling AI | 메인 화면 루프 애니메이션 생성 |
| Claude | 시스템 설계 검토, 기획 정리, 코드 구현 보조, 측정 도구 제작 |
| DeepSeek | 명세가 확정된 반복 구현 |

**반복 구현이나 명세가 명확한 작업은 AI에 위임하고, 시스템 설계·아키텍처
결정·핵심 기획과 같이 판단이 필요한 작업은 직접 수행하는 원칙으로 개발을
진행하였다.** 모든 결과물은 직접 검토·수정한 뒤 프로젝트에 반영했다.

---

## 8. 게임 안의 AI (설계)

> 개발 도구로서의 AI와 별개로, **게임의 소재 자체가 AI**다.

이 게임의 AI는 머신러닝이 아니라 **규칙 기반 결정 엔진**이다. 플레이어가 짠
규칙을 대원이 해석해 행동한다.

### 3축 판단 파이프라인

```
TARGET(누구를 쫓는가) → POSITION(어디에 서는가) → DOCTRINE(언제 무엇을 하는가)
                                                      ↓
                                              직업 기본기 (전부 어긋났을 때)
```

각 축에서 **슬롯을 위에서부터 읽어 처음 성립하는 하나**만 고른다. 그래서
**순서가 곧 전략**이다. 같은 두 장도 어느 칸에 두느냐에 따라 완전히 다른
대원이 된다.

### 폴스루(fall-through)

조건이 참이어도 그 행동이 실제로 불가능하면(대상이 없거나, 이동할 칸이 없으면)
다음 슬롯으로 내려간다. 이게 없으면 조건이 `항상`인 카드 하나가 아래 칸을 전부
죽인다.

성립하지 않은 슬롯은 **이유와 함께 기록**되어 전투 화면에 표시된다
("조건 불성립" / "위가 성립" / "대상 없음"). 성립한 것만 보여 주면 플레이어는
"왜 2번이 안 걸렸지"를 답할 수 없고, 그러면 자기 교리가 틀린 건지 게임이
이상한 건지 구별하지 못한다. **AI를 설계하는 게임에서 그건 치명적이다.**

### 완전 결정론

전투에 난수가 **0개**다.

- 행동 순서는 `Unit.index` (예외 하나: 따라다니는 대원은 대상 뒤에 선다)
- 모든 동점은 index 와 방향 상수 순서로 끊는다
- HP 계산은 정수 연산만 쓴다

같은 편성은 항상 같은 전개가 나온다. 덕분에 **(a) 영상 재촬영이 가능하고
(b) 밸런스를 시뮬레이션으로 잴 수 있다.** 5절의 측정 도구가 성립하는 근거가
바로 이것이다.

---

## 9. 외부 에셋 및 라이선스

전체 목록은 저장소의 `CREDITS.md`에 있다. 요약:

| 분류 | 항목 | 출처 | 라이선스 |
|---|---|---|---|
| 엔진 | Godot Engine 4.7.1 | godotengine.org | MIT |
| 폰트 | 빛의 계승자 Bold | 넥슨 | SIL OFL 1.1 |
| 폰트 | 던파 비트비트체 / v2 | 넥슨 | SIL OFL 1.1 |
| 폰트 | Pretendard 1.3.9 | orioncactus | SIL OFL 1.1 |
| 폰트 | 둥근모꼴 | 둥근모꼴 프로젝트 | SIL OFL 1.1 |
| 배경음 | 4종 (`opening_theme` `ost2` `boss_theme` `story_stage4`) | Pixabay (`opening_theme`은 PaulYudin) | Pixabay Content License |
| 효과음 | 7종 (`step` `attack_melee` `attack_ranged` `click` `special` `defeat` `opening`) | 제작자 직접 제작 | 본인 저작 |
| 효과음 | 2종 (`typing` `beep`) | Pixabay | Pixabay Content License |
| 효과음 | 6종 | **외부 저작물 아님** — 실행 시점에 코드가 파형을 합성 | — |
| 아트 | 82장 | NovelAI 생성 + 직접 후처리 | — |
| 영상 | 타이틀 루프 1편 | Kling AI 생성 + 직접 편집 | — |

**Pixabay Content License** — 상업적 이용 가능, 출처 표기 의무 없음, 개별
판매·재배포 형태의 이용만 금지. 표기 의무는 없으나 공모전 유의사항에 따라
명시한다. 원본 mp3 에서 저장 형식만 바꿨고(22.05kHz 모노 wav) 편곡·편집은
하지 않았다.

**합성 효과음에 대해**: `hit` `heal` `defend` `buy` `victory` `death` 6종은
파일이 아니다. `core/sfx.gd` 가 실행 시점에 사인파·구형파·필터드 노이즈로
직접 만든다. 고정 시드라 매번 같은 소리가 난다. 외부 저작물이 아니다.

---

# 부록

## 부록 A. Claude 활용 예시

**목적** — 전술 시스템 설계 및 밸런스 검토

```
현재 전술 카드 시스템을 기반으로 플레이어가 선택할 수 있는 전투 스타일을
분석하고, 각 전략이 성립하기 위해 필요한 전술 카드를 제안해 달라.

기존 카드와 역할이 겹치는 경우에는 통합 방안을 제안하고,
부족한 역할은 새로운 전술 카드 형태로 설계해 달라.

제안은 현재 게임의 추적(Target), 위치(Position),
특수 효과(Special), 궁극기(Ultimate) 구조를 유지하는 것을 전제로 한다.
```

**목적** — 죽은 전술 진단

```
지금 전술 중에 [광전] 같이 의미가 없는 전술이 없나? 검토해줘.
시뮬레이션을 돌려서 매력적이지 않은 전술과 그 이유가 뭔지,
어떤 전술이 필요할지를 정리해줘.
```

> 이 지시에서 나온 것이 5절의 `module_lab` 측정 도구다. "검토해 달라"가 아니라
> **"시뮬레이션을 돌려서"**를 명시한 것이 답을 의견에서 수치로 바꿨다.

**목적** — 설계 원칙 지시 (교전 축 개편)

```
뭔가 doctrine에 리턴을 추가하는 순간부터
그건 전술이 아니라 강화가 되니까.

차라리 이런 애매한 건 doctrine에 넣어줘:
  적 HP < 30% → 추격
  적 1명 남으면 → 전원 집중
  아군 1명 남으면 → 결사
  내 진영 안이면 → 방어

어그로 관리나 후퇴 + 방어 증가는 남기는 게 나을 듯. 직관적이라.
```

> 능력치 보상 대신 **읽을 수 있는 조건**을 늘리라는 설계 판단이다. 이 방향으로
> 모듈 4종을 추가했고, 측정으로 두 번 고쳐 전부 0 이상으로 맞췄다.

---

## 부록 B. DeepSeek 위임 명세 (템플릿)

실제 위임에 사용한 프롬프트 템플릿이다.

```
Work on ./delegate-work/<file>

Objective: <한 문장, 단일 결과>

Signature / structure:
    <정확한 시그니처 또는 따라야 할 정확한 형태>

Exact behaviour:
1. <입력 형태> -> <출력 형태>
   example: <구체적 입력> -> <구체적 출력>
2. ...
N. Return <X> for: <모든 실패·경계 사례, 예시와 함께>

Constraints:
- <표준 라이브러리만 / 새 의존성 금지 / 출력 금지 / 기존 서식 유지>
- Do NOT modify <기존 함수, class_name 줄, 다른 항목>.
- Output ONLY this file.
```

버그 수정을 위임할 때는 여기에 세 가지를 더 붙였다.
**증상 원문 · 이미 배제한 원인 · 그 현상을 설명하는 도메인 사실.**
정확히 규정된 버그는 위임할 수 있지만, "안 되는데 고쳐줘"는 위임할 수 없다.

### 경계 값 예시를 양쪽에 두는 이유

한 번은 `multiplier()` 에 "알 수 없는 정수는 1.0을 반환"이라고 적고 예시로 `9`를
줬다. 돌아온 코드는 `> 2` 로 가드했고, `-1` 이 모듈로를 통과해 잘못된 0.5를
반환했다. 이후로는 **모든 경계에 양쪽 예시를 주거나, 경계를 범위로 명시**했다.

---

## 부록 C. Kling AI 프롬프트 (타이틀 영상)

```
A looping animation of the character in a game main screen style.
The scene has a soft ambient motion throughout.
The character is slowly and gently blinking their eyes.
A flickering glitch and holographic distortion effect is actively applied
to the whole screen.
Floating particles are drifting by.
Wide shot, cinematic lighting, 4k.
```

의도적으로 넣은 것은 **glitch / holographic distortion** 이다. 이 게임의 이야기는
"지금 보고 있는 화면이 사실은 무엇인가"에 대한 것이고, 타이틀에서부터 화면이
한 번씩 튀어야 그 복선이 처음부터 깔린다.

---

## 부록 D. NovelAI 태그 (대표)

```
character illustration, sci-fi, anime style,
white hair, blue eyes, military uniform,
holographic display, dark background
```

실제 제작에는 프롬프트를 반복 수정하고 태그를 조정하여 원하는 결과를 얻었으며,
생성된 이미지는 **Clip Studio Paint에서 후처리 및 직접 수정한 뒤** 게임에
적용하였다. 애니메이션이 필요한 개체는 파츠 단위로 분리하고, 분리 과정에서
드러나는 가려진 영역을 직접 그려 넣었다.

---

## 부록 E. 개발 자동화

`push` 한 번이면 브라우저에서 도는 빌드까지 자동으로 간다.
(`.github/workflows/pages.yml`)

```
1. Godot 4.7.1 리눅스 바이너리 + 같은 판 익스포트 템플릿 내려받기
2. --headless --import        (리소스 재임포트)
3. parse_check · headless_test · audit 실행   ← 실패하면 배포 안 함
4. Web 프리셋 익스포트 + pck·wasm 산출 확인
5. GitHub Pages 배포
```

3번을 넣은 이유는 단순하다. **깨진 빌드를 올리는 것은 안 올리는 것보다 나쁘다.**
심사자가 그것을 먼저 본다.

4번의 산출물 확인이 있는 이유: 익스포트는 실패해도 종료 코드 0을 내놓는 경우가
있다. 파일이 실제로 나왔는지 직접 봐야 한다.
