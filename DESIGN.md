# GAMBIT GRID (가제) — 데모 기획서

> 규칙을 짜서 AI를 싸우게 하는 격자 오토배틀러.
> 플레이어는 유닛을 조종하지 않는다. **유닛의 판단 기준을 조종한다.**

작성일: 2026-07-31 / 대상: 사전과제 데모

---

## 0. 한 줄 정의

**"내가 조종하는 건 캐릭터가 아니라 캐릭터의 사고방식이다."**

플레이어는 격자에 유닛을 배치하고, 손에 든 **규칙 카드**를 유닛에 우선순위대로
꽂는다. 전투가 시작되면 개입할 수 없다. 유닛은 매 틱 자기 규칙 목록을 위에서부터
읽어 **처음 조건이 맞는 규칙 하나**를 실행한다. 적도 똑같은 시스템으로 움직이며,
스테이지마다 서로 다른 전략을 갖는다. 그 전략을 읽고 카운터 규칙을 짜는 것이 게임.

조건부 우선순위 목록으로 AI를 짜는 구조 자체는 여러 게임에서 검증됐다.
새로 증명해야 할 것이 아니라서 리스크가 낮다.

---

## 1. 영상 시나리오 역산 (← 이게 스코프를 결정한다)

30~60초 안에 컨셉이 전달되어야 한다. 다음 구성으로 고정하고, **이 컷에 필요 없는
기능은 전부 후순위**로 민다.

| 시간 | 화면 | 목적 |
|---|---|---|
| 0-8s | 전투가 이미 진행 중. 내 궁수가 적 전사에게 달라붙어 얻어맞고 전멸 | 문제 제시 |
| 8-10s | **DEFEAT** | 실패를 각인 |
| 10-22s | 규칙 화면. 궁수의 1번 규칙을 `항상 → 가장 가까운 적 공격` 에서 `적이 2칸 이내 → 후퇴` 로 교체 (카드 2장 이동) | **컨셉 전달 구간** |
| 22-50s | 같은 전투 재시작(동일 배치·동일 적). 이번엔 궁수가 물러나며 쏘고, 전사가 앞을 막는다. 유닛 머리 위에 지금 발동한 규칙 이름이 계속 떠오름 | 인과 증명 |
| 50-57s | 마지막 적에게 궁극기 → 슬로우모션 + 컷인 | 임팩트 |
| 57-60s | **VICTORY** | 마무리 |

### 여기서 나오는 필수 요구사항 (전부 구현 우선순위 최상위)

1. **결정론적 전투.** 난수 0. 같은 배치·같은 규칙 = 항상 같은 결과.
   → "규칙 하나 바꿨더니 결과가 뒤집혔다"가 우연이 아님을 보여줘야 하고,
     영상을 몇 번이든 다시 찍을 수 있어야 한다. 디버깅도 압도적으로 쉬워진다.
2. **발동 규칙 라벨.** 유닛이 행동할 때 머리 위에 `HP<50% → 후퇴` 같은 칩이 뜬다.
   → 이 게임의 유일한 차별점인 "AI의 사고"를 눈에 보이게 만드는 장치.
     구현 비용 대비 영상 효과가 가장 큰 기능. **절대 자르지 말 것.**
3. **규칙 유지 재시작.** 패배 후 배치를 다시 하지 않고 규칙만 고쳐 바로 재도전.
   → 배치를 매번 다시 시키면 60초 안에 루프가 안 돈다.
4. **배속 (1x / 2x / 4x).** 전투가 40틱이면 1x로 16초. 2x로 8초.

---

## 2. 게임 규칙

### 2.1 보드 / 시간
- 격자 **8 × 6**, 타일 64px. 4방향 이동.
- **틱 기반.** 1틱 = 0.4초(1x). 매 틱 모든 유닛이 순서대로 1행동.
  행동 순서는 유닛 인덱스 고정 (난수 없음). 이동/공격은 Tween으로 부드럽게 보간
  → 틱제인데 실시간처럼 보인다.
- **60틱 초과 시 패배 처리.** (양쪽이 서로 카이팅하며 무한 대치하는 교착 방지.
  이 예외를 안 넣으면 반드시 무한 루프가 난다.)

### 2.2 유닛 — 5종 중 3명 편성

| 유닛 | HP | 공격력 | 사거리 | 이동 | 규칙 슬롯 | 역할 |
|---|---|---|---|---|---|---|
| 전사 | 120 | 18 | 1 | 1 | 3 | 벽, 어그로 |
| 궁수 | 70 | 22 | 3 | 1 | 3 | 딜, 카이팅 |
| 악사 | 80 | 8 (힐 20) | 2 | 1 | 3 | 유지력 |
| 암살자 | 60 | 30 | 1 | **2** | 3 | 후열 급습, 유리대포 |
| 방패병 | 180 | 10 | 1 | 1 | 3 | 초벽, 방어 태세 전문 |

유닛 종류를 5종으로 늘리는 비용은 **스탯 테이블 몇 줄**이 전부다(위임 대상).
규칙 엔진은 스탯만 참조하므로 새 유닛 타입이 코드를 건드리지 않는다.
대신 편성에 실제 고민이 생기고, 스테이지마다 정답 조합이 달라져 리플레이가 붙는다.

### 2.2.1 편성 ≠ 배치 (난이도 결정)

이 둘은 비용이 완전히 다르므로 반드시 분리해서 판단한다.

| | 내용 | 비용 |
|---|---|---|
| **편성** | 5종 중 3명 고르기 | 리스트 클릭. **카드 장착 UI와 동일한 인터랙션** → 추가 비용 ≈ 0 |
| **자유 배치** | 격자 아무 데나 드래그 | 유효 칸 하이라이트 + 점유 검사 + 되돌리기 → 하루 |

**채택안:** 편성 O, 자유 배치 X.
내 진영 왼쪽 **2열 × 3행 = 6칸** 중에서 슬롯을 클릭 → 유닛을 클릭해 자리를 지정한다.
전열/후열이라는 전략적 선택은 전부 살아있고, 드래그 구현은 하지 않는다.
조작 문법이 카드 장착과 동일해서 심사자가 새로 배울 것도 없다.

### 2.3 규칙 카드 = (조건 → 행동) 한 쌍

> **중요한 난이도 결정:** 조건 카드와 행동 카드를 따로 뽑아 조립하는 방식이 아니라,
> **완성된 규칙 1개가 카드 1장**이다. UI 비용이 1/3로 줄고, 영상에서 카드 한 장이
> 그대로 한 줄 문장으로 읽힌다. 조합 자유도는 카드 종류 수로 확보한다.

**덱 (구 14장 · 현재 16장 — 회피 폐지, 조율 카드 4장 추가. cards.gd 가 원본)**

| # | 카드 | 조건 | 행동 |
|---|---|---|---|
| 1 | 교전 | 항상 | 가장 가까운 적 공격 |
| 2 | 마무리 | 항상 | HP 가장 낮은 적 공격 |
| 3 | 저격 | 적이 사거리 안 | 가장 먼 적 공격 |
| 4 | 돌격 | 항상 | 적에게 1칸 접근 |
| 5 | 후퇴 | 내 HP < 50% | 적 반대로 1칸 |
| 6 | 결사 | 내 HP < 25% | 적에게 1칸 접근 |
| 7 | 거리 유지 | 적이 2칸 이내 | 적 반대로 1칸 |
| 8 | 반격 | 직전 틱에 피격 | 때린 적 공격 |
| 9 | 회피 | 직전 틱에 피격 | 적 반대로 1칸 |
| 10 | 방어 태세 | 주변 적 2명 이상 | 방어 (피해 50% 감소) |
| 11 | 구호 | 아군 HP < 50% | 가장 HP 낮은 아군 회복 |
| 12 | 사수 | 항상 | 제자리 유지 |
| 13 | 각성 | 궁극기 게이지 만땅 | 궁극기 |
| 14 | 추격 | 적이 사거리 밖 | 적에게 1칸 접근 |

**평가 규칙:** 매 틱, 슬롯 1 → 2 → 3 순으로 조건을 검사하고 **처음 참인 것 하나만**
실행한다. 전부 거짓이면 아무것도 안 한다. → 우선순위가 곧 전략이고,
같은 카드라도 순서를 바꾸면 유닛 성격이 완전히 달라진다. 이게 게임의 코어.

### 2.4 스테이지 (2개 — 7일 일정에 맞춰 3개에서 축소)

| # | 적 구성 | 적 전략 | 요구되는 카운터 |
|---|---|---|---|
| 1 | 전사 ×3 | 돌격 → 교전 | 궁수를 뒤로 빼는 법을 배운다 (튜토리얼 겸 영상용) |
| 2 | 궁수 ×2 + 악사 ×1 | 거리 유지 → 저격 / 구호 | 물러나기만 하면 짐. **암살자 + 추격**으로 후열의 악사를 끊어야 함 |

> 스테이지 3(보스전)은 **시간이 남을 때만** 추가하는 보너스로 내린다.
> 늘릴수록 밸런싱 시간이 선형으로 늘어난다.

각 스테이지의 적 전략은 시작 전 화면에 그대로 공개한다. 숨기면 시행착오 게임이
되고, 공개하면 **추리 게임**이 된다. 후자가 훨씬 좋고 데모에 어울린다.

### 2.5 궁극기
게이지는 준 피해 + 받은 피해로 찬다. `각성` 카드를 꽂은 유닛만 사용 가능.
연출: `Engine.time_scale = 0.2` → 화면 암전 → 컷인(아트 없으면 실루엣+텍스트) →
전체 화면 플래시 → 광역 피해. **아트 없이도 성립하게** 먼저 만들고 아트는 나중에 끼운다.

---

## 3. 구현 난이도

| 항목 | 난이도 | 예상 | 비고 |
|---|---|---|---|
| 격자 렌더 + 좌표 변환 | ★☆☆ | 60줄 | |
| 유닛 이동/공격 Tween | ★★☆ | 120줄 | 틱 끝날 때까지 `await` 동기화가 유일한 함정 |
| 경로 탐색 | ★☆☆ | 20줄 | 내장 `AStarGrid2D` 사용. 직접 짜지 말 것 |
| 전투 코어 (HP/데미지/사망/승패) | ★★☆ | 150줄 | |
| **규칙 엔진 (조건 평가 + 행동 실행)** | ★★☆ | 200줄 | 게임의 심장. 직접 작성 |
| 카드/유닛/스테이지 데이터 테이블 | ★☆☆ | 150줄 | **DeepSeek 위임 대상** |
| 발동 규칙 라벨 팝업 | ★☆☆ | 40줄 | 가성비 1위 |
| **배치 + 카드 장착 UI** | ★★★ | 350줄 | **최대 비용. 여기서 일정이 무너진다** |
| 메인/방법/결과 화면 | ★★☆ | 150줄 | |
| 연출 (셰이크·플래시·데미지 숫자·슬로우) | ★★☆ | 120줄 | 임팩트 대부분이 여기서 나옴 |
| 궁극기 컷인 | ★★☆ | 100줄 | |
| 사운드 | ★☆☆ | 40줄 | |
| **웹 익스포트 + Pages 배포** | ★★★ | — | **난이도가 아니라 리스크. 5장 참조** |

### 카드 UI 비용을 줄이는 법
드래그 앤 드롭은 Godot Control에서 은근히 까다롭고 시간을 많이 먹는다.
**v1은 클릭 방식으로 간다:**
- 왼쪽에 유닛 목록 → 유닛 선택
- 가운데에 그 유닛의 슬롯 3칸 (비어 있으면 점선)
- 오른쪽에 덱 → 카드 클릭 = 선택된 슬롯에 장착
- 슬롯 옆 `▲ ▼ ✕` 로 우선순위 변경 / 제거

영상에서 "카드를 끌어다 놓는" 그림이 꼭 필요하면 **마지막 폴리시 단계에서만**
드래그를 추가한다. 순서가 반대가 되면 안 된다.

---

## 4. 일정 — 달력 11일 / 실작업 7일

작업일 6일을 개발에, 마지막 1일을 **제출물 제작**에 쓴다. 영상 촬영과 PDF 2종은
"개발이 끝나면 알아서 되는 일"이 아니라 반나절씩 잡아먹는 별도 작업이다.

**D1 오전 — 배포 파이프라인 스파이크 (최우선, 타협 불가)**
빈 Godot 씬에 "Hello" 라벨 하나만 띄우고 웹 익스포트 → GitHub Pages 배포 →
**다른 기기 브라우저에서 실제로 열리는지 확인.** 여기가 막히면 나머지 계획이
전부 무의미하다. 마지막 날에 발견하면 게임이 완성돼도 제출을 못 한다.

| 작업일 | 내용 | 끝났을 때 상태 |
|---|---|---|
| **D1** | 배포 스파이크 + 격자 + 유닛 + 틱 루프 + 이동 | Pages에서 점들이 격자 위를 움직인다 |
| **D2** | 전투 코어 (HP/데미지/사망/승패/60틱 제한) | 하드코딩 규칙으로 전투가 끝까지 간다 |
| **D3** | **규칙 엔진** + 카드/유닛/스테이지 테이블 | 카드 목록만 바꾸면 행동이 달라진다 |
| **D4** | 규칙 라벨 · 배속 · 피격 플래시 · 데미지 숫자 · 화면 흔들림 | 🎬 **영상 핵심 구간이 촬영 가능해진다** |
| **D5** | 편성/자리 지정 + 카드 장착 UI + 규칙 유지 재시작 | 게임이 게임이 된다 |
| **D6** | 메인·방법·결과 화면, 스테이지 2개, 밸런싱, 사운드, 궁극기 | 심사자가 처음부터 끝까지 플레이 가능 |
| **D7** | 아트 교체, 최종 배포, **영상 촬영**, PDF 2종 | 제출 |

### 매일 배포한다 — 항상 제출 가능한 상태를 유지
작업일이 끝날 때마다 Pages에 밀어 넣는다. 그러면 어느 날 무슨 일이 터져도
**전날까지의 빌드는 제출할 수 있다.**

특히 **D4가 끝나면 백업 영상을 한 편 찍어 둔다.** 이 시점에 규칙 라벨과 연출이
들어가 있어서 컨셉 전달은 이미 가능하다. UI가 없어도 규칙을 코드로 바꿔가며
찍으면 60초는 나온다. 이 영상은 쓸 일이 없기를 바라는 보험이지, 낭비가 아니다.

### 중단선
- **D4 종료 시점에 규칙 라벨이 안 떠 있으면** → 궁극기와 사운드를 버리고 D5-D6을 UI에 전부 쓴다
- **D5 종료 시점에 카드 UI가 안 끝났으면** → 스테이지를 1개로 줄이고 편성을 고정 편성으로 강등
- 어떤 경우에도 **D7은 건드리지 않는다.** 제출물 제작일을 개발로 잡아먹는 순간 제출 자체가 위험해진다

### 잘라낸 것 (10일 → 7일)
- 스테이지 3개 → **2개**
- 궁극기 컷인 아트 → **플래시 + 슬로우 + 광역 피해**로 대체 (아트는 남으면)
- 자유 격자 배치 → **6칸 슬롯 클릭**
- 카드 드래그앤드롭 → **클릭 + ▲▼**

---

## 5. 리스크

### 🔴 R1. Godot 웹 익스포트 × GitHub Pages — 최대 리스크
Godot 4의 웹 빌드는 원래 `SharedArrayBuffer`를 요구하고, 그건 서버가
`COOP`/`COEP` 헤더를 보내야 동작한다. **GitHub Pages는 그 헤더를 보낼 수 없다.**
과거 수많은 프로젝트가 여기서 막혔다.

**대응 (반드시 Day 0에 검증):**
1. Godot **4.3 이상**을 쓴다. 이 버전부터 스레드 없는 웹 빌드가 가능해졌다.
2. 익스포트 설정에서 **Thread Support를 끈다.** (켜면 Pages에서 안 뜬다)
3. 그래도 안 되면 `coi-serviceworker.js` 셰이브를 `index.html`에 끼운다.
4. 최후의 수단: itch.io(헤더 지원)에 올리고 Pages에는 링크만 둔다.
   — 다만 제출 요건이 Pages를 명시하므로 3번까지 먼저 시도.

부수 이슈: wasm 빌드가 20~40MB라 첫 로딩이 10~20초 걸린다. **로딩 화면 필수.**
심사자가 흰 화면 보고 닫으면 끝이다. 모바일 브라우저는 지원 포기하고 문서에
"데스크탑 브라우저 권장"이라고 명시한다.

### 🟡 R2. 오토배틀러는 화면이 조용하다
점들이 격자에서 서로 툭툭 치는 그림은 생각보다 심심하다. 아트로 해결하려 하면
늦다. **연출 순서를 비용 대비 효과 순으로:**
피격 플래시 → 데미지 숫자 → 화면 흔들림 → 규칙 라벨 → 사망 시 파티클 →
마지막 일격 슬로우+줌 → 궁극기 컷인. 아트는 이 다음이다.

### 🟡 R3. 무한 교착
양쪽 다 `거리 유지`를 들고 있으면 영원히 안 끝난다. 60틱 제한을 **처음부터**
넣는다. 나중에 넣으면 이미 시간을 다 잃은 뒤다.

### 🟡 R4. 규칙이 3슬롯이면 전략이 안 나온다 / 5슬롯이면 이해가 안 된다
3슬롯으로 시작해서 스테이지 3에서만 4슬롯을 준다. 밸런싱은 Day 7에.

### 🟢 R5. 영상 규정
"AI를 이용한 조작·합성 불가"는 **영상**에 대한 조항이다. 게임 에셋의 AI 생성은
오히려 권장된다(단 출처 기재 필수). 영상은 무편집 실플레이 화면으로 녹화하되,
컷 편집·자막·BGM은 합성이 아니므로 문제없다. **화면 녹화 원본은 보관해 둘 것.**

---

## 6. 에셋 / 사운드 출처

라이선스 리스크가 0인 것부터 고른다. 제출물 4번 문서에 전부 기재해야 하므로
**쓰는 순간 바로 기록**한다.

| 종류 | 추천 | 라이선스 | 비고 |
|---|---|---|---|
| 효과음 | **kenney.nl** | CC0 | 출처 표기 의무조차 없음. 1순위 |
| 효과음 | **jsfxr / ChipTone** | 내가 생성 | 레트로 8bit. 라이선스 문제 원천 차단 |
| 효과음 | freesound.org | 음원별로 다름 | CC0 필터 걸고 받을 것. 개별 확인 필수 |
| BGM | **Incompetech (Kevin MacLeod)** | CC-BY | 표기만 하면 됨. 가장 안전 |
| BGM | FreePD / Pixabay Music | PD / Pixabay | |
| 아트 | NovelAI (유료) | 출력물 사용 가능 | 문서에 "NovelAI로 생성" 명시 |
| 폰트 | 프리텐다드 / 넥슨 폰트 | OFL 등 | 한글 필수. 라이선스 확인 |

**개발 중에는 전부 단순 도형으로 간다.** 유닛 = 색 원 + 팀 색 테두리 + HP 바.
아트는 Day 9에 텍스처만 갈아 끼울 수 있게 처음부터 노드를 분리해 둔다.

---

## 7. DeepSeek 위임 대상

원칙: **테이블은 위임하고, 기계는 직접 짠다.**

| 위임 | 대상 |
|---|---|
| ✅ | `data/cards.gd` — 규칙 카드 정의 테이블 (16장) |
| ✅ | `data/units.gd` — 유닛 스탯 테이블 |
| ✅ | `data/stages.gd` — 스테이지 3개 배치/적 규칙 데이터 |
| ✅ | 조건 판정 순수 함수 모음 (입출력 예시를 전부 명시할 수 있음) |
| ❌ | 규칙 엔진, 틱 루프, 전투 상태 기계, UI, 연출 — 판단이 필요한 영역 |

**GDScript 위임 시 함정 (실측):** 사전에 `{ "key": v }` 라고 못 박지 않으면
`{ key = v }` 로 써서 온다. 이건 String이 아니라 **StringName** 키를 만든다.
Godot이 둘을 같게 취급해서 우연히 동작하고, 그래서 리뷰에서 안 걸린다.
스펙에 **리터럴 표기까지** 고정할 것.

---

## 8. 제목 후보

- **GAMBIT GRID** — 직관적, 영문 로고 뽑기 쉬움
- **RULESET** — 짧고 강함
- **조건반사** — 한글, 이 게임의 원리 그 자체
- **전술 프로토콜**

---

## 9. 결정 / 미결

**결정됨 (2026-07-31)**
- 기간: 달력 11일, 실작업 7일 → 개발 6일 + 제출물 1일
- 편성 O (5종 중 3명), 자유 격자 배치 X (6칸 슬롯 클릭)
- 스테이지 2개, 궁극기는 아트 없는 연출 버전

**미결**
1. 제목 (8장 후보)
2. 개인 참여 여부 → 2인 이상이면 제출물 5번(팀원 롤 기술서)이 추가로 필요
3. Godot 버전 확인 — **4.3 미만이면 업그레이드부터.** D1 스파이크 전에 확인할 것
